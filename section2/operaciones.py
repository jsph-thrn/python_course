# Suma en python

# Declarando x

x = 10

# Declarando y

y = 2

# Sumando x y y

z = x + y

# Imprimiendo los valores en el orden en que fueron declaradas

print(x)
print(y)
print(z)

# Los valores de x, y, y z son almacenados en una posición de memoria en la memoria RAM cada una

# La función id() permite conocer la posición de memoria de la variable, este valor cambiará cada
# vez que se declare la variable

print(id(x))
print(id(y))
print(id(z))
